spring:
  application:
    name: movieverse
  datasource:
    url: jdbc:postgresql://localhost:5432/movieverse
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: db/changelog/changelog-master.yaml